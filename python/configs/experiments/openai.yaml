name: openai-demo
backend:
  kind: openai
  model: gpt-4o-mini
  # api_key: set via OPENAI_API_KEY env var
  # For compatible servers (vLLM/Ollama):
  # base_url: http://localhost:11434/v1
tools:
  - kind: mcp
orchestrator:
  url: http://localhost:8765
  pipe: "\\\\.\\pipe\\civv_llm"

